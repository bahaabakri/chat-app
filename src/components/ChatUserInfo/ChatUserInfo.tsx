import styles from './ChatUserInfo.module.scss'
const ChatUserInfo: React.FC = () => {
    return (
      <section className={styles['user-info-wrapper']}>
      </section>
    );
  };
  
  export default ChatUserInfo;