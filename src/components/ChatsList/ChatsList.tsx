import styles from './ChatsList.module.scss'
const ChatsList: React.FC = () => {
    return (
      <section className={styles['chat-lists-wrapper']}>
      </section>
    );
  };
  
  export default ChatsList;