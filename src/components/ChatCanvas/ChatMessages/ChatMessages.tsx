import styles from './ChatMessages.module.scss'
const ChatMessages: React.FC = () => {
    return (
      <section className={styles['chat-messages-wrapper']}>

      </section>
    );
  };
  
  export default ChatMessages;